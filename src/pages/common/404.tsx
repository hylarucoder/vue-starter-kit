import {defineComponent, ref, withModifiers} from "vue";

export default defineComponent({
    name: "Page404",
    setup: function () {
        return () => (
            <div>
                404
            </div>
        )
    },
});
