import {defineComponent, ref, withModifiers} from "vue";

export default defineComponent({
    name: "Page403",
    setup: function () {
        return () => (
            <div>
                403
            </div>
        )
    },
});
